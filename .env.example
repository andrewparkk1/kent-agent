# Kent hosted infrastructure (server-side only — users never set these)
# These are for the Kent team / self-hosting only.

CONVEX_URL=
E2B_API_KEY=
E2B_TEMPLATE_ID=
TELEGRAM_BOT_TOKEN=

# Users don't need any env vars. AI keys are collected during `kent init`
# and stored encrypted in Convex.
